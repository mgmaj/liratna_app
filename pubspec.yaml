name: liratna_app
description: تطبيق بسيط لمتابعة أسعار الليرة السورية
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15
  flutter_local_notifications: ^16.1.0
  workmanager: ^0.5.1

flutter:
  uses-material-design: true